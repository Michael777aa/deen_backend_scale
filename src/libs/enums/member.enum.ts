export enum MemberProvider {
  kakao = "kakao",
  naver = "naver",
  custom = "custom",
  google = "google",
}
export enum MemberType {
  USER = "USER",
  OWNER = "OWNER",
}

export enum MemberStatus {
  ACTIVE = "ACTIVE",
  BLOCK = "BLOCK",
  DELETE = "DELETE",
}
